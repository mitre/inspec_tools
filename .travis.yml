---
sudo: false
language: ruby
cache: bundler

bundler_args: --without integration --without tools

before_install:
- gem update --system
- gem update bundler
- gem update rake
- gem --version
- bundle install

script:
  - bundle exec rake test
  - bundle exec exe/inspec_tools --help
  - bundle exec exe/inspec_tools --version

matrix:
  include:
  - rvm: 2.3
  - rvm: 2.4
  - rvm: 2.5
  - rvm: 2.6
